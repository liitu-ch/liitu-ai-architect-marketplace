---
description: Resolve a GitHub issue end to end — spec correction, regression test, seed data, fix, review, commit
---

Use the `fix-issue` skill from the `ai-architect-dev-tools` plugin to resolve the issue passed as argument: load and qualify the issue (run the `issue` skill first when the root cause analysis is missing), correct the use case, requirements, business rules, or entity model when the root cause lies in the specification, plan the fix in the use case's implementation plan, write the regression test first and add mock seed data where reproduction needs it, implement the fix under the project guidelines, verify the acceptance criteria, sync statuses, review the branch with `code-review`, and commit with `commit`.

$ARGUMENTS
