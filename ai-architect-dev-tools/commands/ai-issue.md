---
description: Capture a new issue with root cause analysis (spec, code, tests, review) and create it on GitHub
---

Use the `issue` skill from the `ai-architect-dev-tools` plugin to turn the reported problem or testing feedback into a GitHub issue: ask about test data and the affected entity, locate the governing use case, requirements, and business rules, analyse the root cause (spec gap or contradiction, code defect, regression), explain why the tests and the code review did not catch it, classify the finding as Bug or Change Request together with the user, and create the issue with the project's issue template via `gh`.

$ARGUMENTS
