---
description: Review current changes or a PR against project conventions and recurring defect classes
---

Use the `code-review` skill from the `ai-architect-dev-tools` plugin to review the current changes (or the PR/branch passed as argument) against the project's binding conventions — language rules and glossary, i18n completeness, UI consistency, documentation status sync — and recurring defect classes, and report verified findings with severity and concrete fixes.

$ARGUMENTS
